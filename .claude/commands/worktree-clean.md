You are executing `/worktree-clean` for `daniel-klesc/predictor`.

## Purpose

Clean up stale git worktrees, orphaned disk directories, and local `worktree-agent-*` branches left over from `/issues-build` runs.

## Instructions

Load `.agents/skills/worktree-clean/SKILL.md` and follow it. Pass any arguments from the user through unchanged.
