You are executing `/branch-prune` for `daniel-klesc/predictor`.

## Purpose

Delete local branches whose remote tracking branch has been removed from origin. Fetches and prunes stale remote-tracking refs, identifies gone branches, flags branches with unique commits, and deletes safe branches after confirmation.

## Instructions

Load `.agents/skills/branch-prune/SKILL.md` and follow it. Pass any arguments from the user through unchanged.
