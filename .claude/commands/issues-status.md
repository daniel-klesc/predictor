You are executing `/issues-status` for `daniel-klesc/predictor`.

## Purpose

Backlog snapshot: ready issues, in-progress, open PRs, needs-definition.

## Instructions

Load `.agents/skills/issues-status/SKILL.md` and follow it. Pass user arguments through.
